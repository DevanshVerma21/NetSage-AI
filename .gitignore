# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/

# Secrets — never commit
.env
.env.local
*.key

# Runtime records written by the API (diagnoses, human reviews, simulated fix runs).
# Ignored deliberately: they are per-run output, not source, and a demo would otherwise
# dirty the working tree. The Responsible-AI log is a separate, deliberate export under
# reports/ rather than these raw files.
data/diagnoses.json
data/reviews.json
data/fix_runs.json

# Node / frontend
node_modules/
dist/
.vite/

# OS
Thumbs.db
desktop.ini
.DS_Store


AI_Problem Statement.docx
