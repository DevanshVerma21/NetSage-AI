# NetSage AI — backend dependencies
# Pinned loosely; all of these were already present in the dev environment.

fastapi>=0.115
uvicorn>=0.30
pydantic>=2.8
pydantic-settings>=2.4
python-dotenv>=1.0

# Default live LLM provider (Google Gemini)
google-genai>=1.60

# Second live provider, added in Phase 6 when the Gemini free tier's daily quota ran out
# mid-batch. Optional: the prototype runs on gemini or mock without it.
anthropic>=0.75

# Testing
pytest>=8.0
httpx>=0.27          # FastAPI TestClient transport
