# Responsible AI — what humans corrected

Generated by `python -m backend.scripts.build_responsible_ai` from `data/responsible_ai_log.json`. Do not edit by hand.

Generated at: 2026-08-28T12:24:55.776295+00:00

## How to read this

Each entry keeps three things apart:

1. **What the AI produced** — Gemini's root cause, its own confidence, the confidence the deterministic caps actually allowed, and its citations with the verifier's verdict on each one.
2. **What the human decided** — the verdict, the correction, the reason code and the reviewer's own notes, exactly as recorded. A correction never overwrites the AI's proposal; both are stored.
3. **The lesson** — derived mechanically from (1) and (2). It is labelled `lesson_source` in the JSON so it cannot be mistaken for something a reviewer wrote.

No diagnosis in this file was applied to a device. NetSage AI simulates fixes only, and `applied` stays false until a human runs the simulator explicitly.

## Totals

| Metric | Value |
| --- | --- |
| reviews total | 10 |
| reviews accepted | 5 |
| reviews edited | 2 |
| reviews rejected | 3 |
| corrections documented (edited + rejected) | 5 |
| corrections required by the phase spec | 5 |

## Corrections

### 1. CASE-005 — human decision: **REJECTED**

- diagnosis: `DIAG-5b506d4b02ab` · review: `REV-4109e8f97d3c` · recorded 2026-08-28T11:54:10+00:00
- mechanical evaluation: _not recorded_ · evidence _not recorded_ · reconciliation _not recorded_
- confidence: model **_not recorded_** → effective **_not recorded_**
- applied to any device: **no**

**AI proposal**

- root cause: There is a native VLAN mismatch on the Gi0/24 trunk link between SW1 and SW2. SW1 is configured with native VLAN 1 while SW2 is configured with native VLAN 99, causing CDP mismatch warnings and untagged traffic leakage.
- category / OSI: VLAN / L2
- next command: show interfaces trunk
- provider / model: _not recorded_ / _not recorded_

**AI citations**: none recorded.

**Human correction**

- reviewer: human-reviewer
- reason code: other
- corrected root cause: _not recorded_
- corrected category / OSI: _not recorded_ / _not recorded_
- corrected fix steps: _not recorded_
- reviewer notes: The AI claims this configuration causes "untagged traffic leakage." In reality, on enterprise switches (like Cisco running PVST+), Spanning Tree detects the native VLAN mismatch (PVID inconsistency) and places the port into a broken/blocking state (*PVID_Inc) for those specific VLANs. This actively prevents traffic leakage. The AI is misrepresenting how STP handles misconfigurations.

**Lesson** _(derived from the stored review and the deterministic checks)_

A reviewer rejected the AI's root cause outright, so the proposal was never eligible for a simulated fix. Reviewer reason code: other.

### 2. CASE-019 — human decision: **REJECTED**

- diagnosis: `DIAG-a16ae59c00a0` · review: `REV-504fefdd67dc` · recorded 2026-08-28T11:54:44+00:00
- mechanical evaluation: _not recorded_ · evidence _not recorded_ · reconciliation _not recorded_
- confidence: model **_not recorded_** → effective **_not recorded_**
- applied to any device: **no**

**AI proposal**

- root cause: GigabitEthernet0/2 on SW-DEPOT, which connects to DNS server SRV-DNS (192.168.180.50), is administratively down. This breaks physical connectivity to the DNS server, causing all name resolution requests from clients in VLAN 180 to fail.
- category / OSI: INTERFACE_CONFIG / L1
- next command: show interfaces GigabitEthernet0/2
- provider / model: _not recorded_ / _not recorded_

**AI citations**: none recorded.

**Human correction**

- reviewer: human-reviewer
- reason code: wrong_root_cause
- corrected root cause: _not recorded_
- corrected category / OSI: _not recorded_ / _not recorded_
- corrected fix steps: _not recorded_
- reviewer notes: The AI artificially limits the blast radius, stating it causes requests "from clients in VLAN 180 to fail." If GigabitEthernet0/2 connects to the primary DNS server (SRV-DNS), shutting this port down breaks name resolution for the entire network, not just VLAN 180. The AI failed to identify the systemic impact.

**Lesson** _(derived from the stored review and the deterministic checks)_

A reviewer rejected the AI's root cause outright, so the proposal was never eligible for a simulated fix. Reviewer reason code: wrong_root_cause.

### 3. CASE-012 — human decision: **REJECTED**

- diagnosis: `DIAG-59a278ce7cc0` · review: `REV-21209c7eee48` · recorded 2026-08-28T11:55:16+00:00
- mechanical evaluation: PARTIAL · evidence passed · reconciliation agree
- confidence: model **high** → effective **high**
- applied to any device: **no**

**AI proposal**

- root cause: DHCP pool LAB-POOL on R-CAMPUS is configured with default-router 192.168.111.1, which is outside the local 192.168.110.0/24 subnet. As a result, PC-LEASED receives an unreachable default gateway address, preventing it from sending traffic off-subnet.
- category / OSI: DHCP / L3
- next command: show ip dhcp pool LAB-POOL
- proposed fix: 1. [R-CAMPUS] configure terminal; ip dhcp pool LAB-POOL; default-router 192.168.110.1; end — Correct the default-router setting in DHCP pool LAB-POOL to match R-CAMPUS's Gi0/0 interface IP address (192.168.110.1). (risk=low)
- proposed fix: 2. [R-CAMPUS] clear ip dhcp binding * — Clear existing active leases so DHCP clients rebind and pick up the updated default gateway address. (risk=medium)
- proposed fix: 3. [PC-LEASED] ipconfig /release; ipconfig /renew — Force PC-LEASED to release its current lease and request new parameters from the updated DHCP pool. (risk=low)
- provider / model: gemini / gemini-3.6-flash

**AI citations, as checked by the deterministic verifier**

| # | source command | verified | excerpt | why it failed |
| --- | --- | --- | --- | --- |
| 1 | `show running-config | section dhcp` | yes | default-router 192.168.111.1 | — |
| 2 | `show ip interface brief` | yes | GigabitEthernet0/0 192.168.110.1 YES manual up up | — |
| 3 | `ipconfig /all` | yes | Default Gateway.................: 192.168.111.1 | — |

**Human correction**

- reviewer: human-reviewer
- reason code: out_of_scope
- corrected root cause: _not recorded_
- corrected category / OSI: _not recorded_ / _not recorded_
- corrected fix steps: _not recorded_
- reviewer notes: The AI states the PC "receives an unreachable default gateway address." This is technically inaccurate for how modern operating systems handle DHCP. If a DHCP server assigns a default gateway that falls outside the client's provided subnet mask, the OS (e.g., Windows/Linux) will outright reject the gateway and fail to install a 0.0.0.0/0 route at all, rather than installing it and finding it unreachable.

**Lesson** _(derived from the stored review and the deterministic checks)_

A reviewer rejected the AI's root cause outright, so the proposal was never eligible for a simulated fix. The model claimed HIGH confidence and no cap applied, so nothing automated flagged this one — the human review is the only thing that caught it. This is the failure mode the mandatory review gate exists for. Reviewer reason code: out_of_scope.

### 4. CASE-010 — human decision: **EDITED**

- diagnosis: `DIAG-8314f663dfa7` · review: `REV-629c944257f7` · recorded 2026-08-28T11:56:10+00:00
- mechanical evaluation: _not recorded_ · evidence _not recorded_ · reconciliation _not recorded_
- confidence: model **_not recorded_** → effective **_not recorded_**
- applied to any device: **yes**

**AI proposal**

- root cause: PC-SUP is configured with a /25 subnet mask (255.255.255.128) instead of the correct /24 subnet mask (255.255.255.0). As a result, PC-SUP treats any host in VLAN 90 with an IP address above 192.168.90.127 (such as 192.168.90.200) as off-subnet and routes traffic for them through its default gateway rather than resolving them via ARP locally.
- category / OSI: INTERFACE_CONFIG / L3
- next command: show ip interface Vlan90
- provider / model: _not recorded_ / _not recorded_

**AI citations**: none recorded.

**Human correction**

- reviewer: human-reviewer
- reason code: fix_steps_incorrect
- corrected root cause: _not recorded_
- corrected category / OSI: _not recorded_ / _not recorded_
- corrected fix steps: Add that once the gateway receives the misdirected traffic, it will either drop it (due to firewall hairpinning rules) or send an ICMP Redirect back to the PC, causing asymmetric routing and breaking the TCP handshake.
- reviewer notes: The deterministic rule engine already flagged this as PARTIAL. The AI correctly identifies that the PC treats hosts above .127 as off-subnet and forwards them to the gateway. However, it fails to explain why the connection ultimately fails.

**Lesson** _(derived from the stored review and the deterministic checks)_

A reviewer kept the diagnosis but corrected fix steps. Reviewer reason code: fix_steps_incorrect.

### 5. CASE-014 — human decision: **EDITED**

- diagnosis: `DIAG-4107d732a035` · review: `REV-b86fee59f20f` · recorded 2026-08-28T11:56:47+00:00
- mechanical evaluation: _not recorded_ · evidence _not recorded_ · reconciliation _not recorded_
- confidence: model **_not recorded_** → effective **_not recorded_**
- applied to any device: **no**

**AI proposal**

- root cause: SW-CAMP interface Vlan130 is missing an 'ip helper-address 192.168.131.10' configuration after being reloaded from an older configuration file. Because the DHCP server resides on a different subnet (VLAN 131), DHCP broadcast requests from clients in VLAN 130 are not forwarded across the Layer 3 boundary.
- category / OSI: DHCP / L3
- next command: show running-config interface Vlan130
- provider / model: _not recorded_ / _not recorded_

**AI citations**: none recorded.

**Human correction**

- reviewer: human-reviewer
- reason code: wrong_osi_layer
- corrected root cause: _not recorded_
- corrected category / OSI: _not recorded_ / _not recorded_
- corrected fix steps: Expand the explanation to define the actual mechanism: clarify that without the ip helper-address, the router will simply drop the Layer 2 broadcast. The command is specifically required to intercept the UDP Port 67 broadcast and repackage it as a directed unicast packet toward the DHCP server.
- reviewer notes: he AI accurately identifies the missing command but provides a slightly vague explanation ("requests... are not forwarded across the Layer 3 boundary").

**Lesson** _(derived from the stored review and the deterministic checks)_

A reviewer kept the diagnosis but corrected fix steps. Reviewer reason code: wrong_osi_layer.

